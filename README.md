# Library_Management_System
Desktop application for managing a library system with users, books, roles, and database integration.

This project is a desktop application built in C# for managing a library system. It includes user authentication, multiple roles (admin, librarian, user), and database integration for storing books and user data. The application also includes automated tests to ensure correct functionality.
